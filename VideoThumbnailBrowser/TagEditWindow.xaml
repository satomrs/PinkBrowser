<Window x:Class="VideoThumbnailBrowser.TagEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="タグを編集" Height="220" Width="420"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#252526">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="タグ（カンマ区切りで複数入力できます）" Foreground="White" Margin="0,0,0,4" />
        <TextBox x:Name="TagInputBox" Grid.Row="1" Padding="6,4" Background="#3C3C3C" Foreground="White"
                 BorderBrush="#3A3A3A" Margin="0,0,0,8" />

        <TextBlock Grid.Row="2" Text="既存のタグ（クリックで追加）" Foreground="Gray" Margin="0,0,0,4" />
        <ItemsControl x:Name="ExistingTagsControl" Grid.Row="3">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Button Content="{Binding}" Margin="0,0,4,4" Padding="6,2" Click="OnExistingTagClick" />
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="OK" Width="80" Margin="0,0,8,0" Padding="4" IsDefault="True" Click="OnOkClick" />
            <Button Content="キャンセル" Width="80" Padding="4" IsCancel="True" />
        </StackPanel>
    </Grid>
</Window>
